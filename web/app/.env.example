# Copy to .env.local for local dev (Vite loads it automatically, and it's
# gitignored). Unset in CI/production until service/app.py is deployed and
# its URL is known -- the Squad Optimizer view degrades to an explanation
# rather than a network error when this is absent (see data/optimizer.ts).
VITE_OPTIMIZER_API_URL=http://localhost:8000
